# Abhishek Pandey — React Portfolio

Modern React/Vite portfolio with separate components, sections, data modules, light/dark theme, responsive UI, favicon, resume and Netlify configuration.

## Run
npm install
npm run dev

## Production build
npm run build

Deploy the `dist` folder to Netlify, or connect the repository with build command `npm run build` and publish directory `dist`.
