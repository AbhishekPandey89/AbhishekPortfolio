import { personal } from "../../data/personal"; import { projects } from "../../data/projects";
export default function Projects(){return <section id="projects" className="section projects reveal"><div className="label">05 / PROJECTS</div><div className="content"><div className="project-head"><div><p className="kicker">Selected work</p><h2>Things I've <span>built.</span></h2></div><a className="github-all" href={personal.github} target="_blank" rel="noreferrer">View GitHub ↗</a></div><div className="project-list">{projects.map(p=><article className="project" key={p.no}><div className="project-number">{p.no}</div><div className="project-main"><div className="project-meta">{p.stack.join(" · ")}</div><h3>{p.title}</h3><p>{p.desc}</p><div className="project-links">{p.links.length?p.links.map(l=><a key={l[0]} href={l[1]} target="_blank" rel="noreferrer">{l[0]} ↗</a>):<span className="muted-link">Project details available on request</span>}</div></div><span className="project-arrow">↗</span></article>)}</div></div></section>}
