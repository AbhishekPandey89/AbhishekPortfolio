import { useState } from "react";
import ThemeToggle from "../ThemeToggle/ThemeToggle";
export default function Navbar() { const [open, setOpen] = useState(false); const items = ["about", "services", "skills", "experience", "projects", "education", "contact"]; const go = id => { document.getElementById(id)?.scrollIntoView({ behavior: "smooth" }); setOpen(false) }; return <header className="nav"><a className="logo" href="#top">ABHISHEK<span>.</span></a><button className="hamb" onClick={() => setOpen(!open)} aria-label="Toggle menu">{open ? "×" : "☰"}</button><nav className={open ? "show" : ""}>{items.map(item => <a key={item} href={`#${item}`} onClick={e => { e.preventDefault(); go(item) }}>{item}</a>)}<a className="resume-nav" href="/resume/Abhishek-Pandey-Resume.pdf" target="_blank" rel="noreferrer">Resume</a><ThemeToggle /></nav></header> }
